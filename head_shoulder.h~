bool detection_pcl(pcl::PointCloud < pcl::PointXYZ >::Ptr cloud, int pos,
                   int posz, bool head_before);
void detection_dist(cv::Mat dist_mat, int center_x, int center_y,
                    float alt_head, float alt_suelo);
